vlm_parquet:
  # Legacy alias. Prefer data/configs/gui_grounding_table1.yaml; both resolve
  # to the paper-sized mixture selected by LLADAO_GUI_GROUNDING_DIR.
  dataset_names:
  - gui_grounding_120k
  image_transform_args:
    image_stride: 14
    max_image_size: 980
    min_image_size: 378
    max_pixels: 2_007_040
  frame_sampler_args:
    max_num_frames: 1
    min_num_frames: 1
  is_mandatory: true
  num_used_data:
  - null
  weight: 1
